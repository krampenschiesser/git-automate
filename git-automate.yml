concurrency: 4
githubToken: ${env:GITHUB_TOKEN}
opencode:
  url: ${env:OPENCODE_URL}
  pw: ${env:OPENCODE_PW}
git:
  repository: https://github.com/krampenschiesser/git-automate
  projectId: 5
  issueProvider: github
