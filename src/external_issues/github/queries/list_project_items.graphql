query ListProjectItems($id: ID!) {
    node(id: $id) {
        ... on ProjectV2 {
            items(first: 100) {
                nodes {
                    id
                    content {
                        __typename
                        id
                        ... on Issue {
                            number
                        }
                        ... on PullRequest {
                            number
                        }
                    }
                }
            }
        }
    }
}
